import React, { useEffect } from "react";

function Logout() {
  // const history = useHistory();
  console.log("logout!");

  return (
    <div>
      <h1>TEST</h1>
    </div>
  );
}

export default Logout;
